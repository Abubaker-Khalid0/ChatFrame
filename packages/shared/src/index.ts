/**
 * @chatframe/shared — common definitions used by the backend and frontend.
 * Barrel export (FR-012). Schemas (e.g. HealthStatus) are added in later phases.
 */
export * from './constants';
export * from './types';
export * from './schemas';
